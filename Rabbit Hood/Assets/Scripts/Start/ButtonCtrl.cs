using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonCtrl : MonoBehaviour
{
    public static bool OpenButton = true;

    private void Start()
    {
        OpenButton = true;
    }
    // Start is called before the first frame update

}
